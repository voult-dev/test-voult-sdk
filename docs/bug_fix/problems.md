## List of problems found across this test codebase

### API
- `Invalid CSRF Token` message test app login and signup
- Password validation is too strict 
        - (LATER) Let developers be able to choose the password pattern to implement in their app and the exact pattern they chose will be used in the app.
- Sign out sends out signout message even when their is no signed in user. (FIXED)