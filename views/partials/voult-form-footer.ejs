<input type="hidden" name="_redirect" value="1">
<p class="text-muted small mb-0 mt-3">
  POSTs to <code>/voult/signin/*</code> or <code>/voult/signup/*</code> (same SDK handlers as <code>/signin/*</code> and <code>/signup/*</code>) with <code>_redirect=1</code> so tokens are stored in the Express session.
</p>
