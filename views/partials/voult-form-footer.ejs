<input type="hidden" name="_redirect" value="1">
<p class="text-muted small mb-0 mt-3">
  Posts to the same routes used for JSON tests (<code>/signup/*</code>, <code>/signin/*</code>) with <code>_redirect=1</code> so the SDK session is stored in your browser session.
</p>
